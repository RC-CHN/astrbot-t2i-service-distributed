PORT=8999

# 图片生命时间（小时），默认24小时
# 超过这个时间的图片文件将被自动清理
IMAGE_LIFETIME_HOURS=24

# Redis 缓存连接
REDIS_URL=redis://localhost:6379/0

# Redis 内存上限（默认 512mb）
REDIS_MAXMEMORY=512mb

# Redis 淘汰策略（默认 volatile-ttl）
REDIS_MAXMEMORY_POLICY=volatile-ttl
